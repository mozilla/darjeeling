name: metropolis

instances: 1

framework:
    type: node
    runtime: node010

mem: 128

hooks:
    pre-push:
        - ./deploy.sh

processes:
    web: DEBUG=0 npm run-script start

ignores:
    [".git", "node_modules/*"]
